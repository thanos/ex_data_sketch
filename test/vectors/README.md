# Test Vectors

This directory contains deterministic test vectors for verifying sketch algorithm
correctness and cross-backend parity.

## Naming Convention

Vector files are named: `{sketch}_{version}_{description}.bin`

Examples:
- `hll_v1_empty.bin` - Empty HLL sketch state
- `cms_v1_1000items.bin` - CMS sketch with 1000 deterministic items

## Versioning

Each vector file includes the sketch binary format version in its name.
When the binary layout changes, new vectors are generated with the new version
and old vectors are retained for backwards-compatibility testing.

## Generation

Vectors are generated by deterministic input sequences using seeded hashing.
The exact generation procedure is documented alongside each vector file.

## Status

Phase 0: Directory created, no vectors yet. Vectors will be added in Phase 1
when full algorithm implementations are complete.
